import { ArrayOfIntervalPipe } from './array-of-interval.pipe';

describe('ArrayOfIntervalPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayOfIntervalPipe();
    expect(pipe).toBeTruthy();
  });
});
